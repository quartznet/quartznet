# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [DatabaseIntegrationGitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_pr-integration-sqlite --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: pr-integration-sqlite

on:
  pull_request:
    branches:
      - main
      - '3.x'
    paths:
      - '**/*'
      - '!docs/**/*'
      - '!package.json'
      - '!package-lock.json'
      - '!readme.md'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            10.0
      - uses: actions/checkout@v6
      - name: 'Run: Compile, IntegrationTest (sqlite)'
        run: ./build.cmd Compile IntegrationTest --database sqlite
        env:
          QUARTZ_TEST_DATABASE: sqlite
