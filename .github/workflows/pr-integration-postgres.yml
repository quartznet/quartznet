# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [DatabaseIntegrationGitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         fallout --generate-configuration GitHubActions_pr-integration-postgres --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: pr-integration-postgres

on:
  pull_request:
    branches:
      - main
      - v4
      - '3.x'
    paths:
      - '**/*'
      - '!docs/**/*'
      - '!package.json'
      - '!package-lock.json'
      - '!readme.md'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: 'Setup: .NET SDK'
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json
      - name: 'Restore: dotnet tools'
        run: dotnet tool restore
      - name: 'Run: Compile, IntegrationTest'
        run: dotnet fallout Compile IntegrationTest
        env:
          Database: postgres
