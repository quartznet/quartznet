# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [CustomGitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_pr-tests-unit --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: pr-tests-unit

on:
  pull_request:
    branches:
      - main
      - v4
      - '3.x'
    paths:
      - '**/*'
      - '!docs/**/*'
      - '!package.json'
      - '!readme.md'

concurrency:
  group: ${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            10.0
      - uses: actions/checkout@v4
      - name: 'Run: Compile, UnitTest'
        run: ./build.cmd Compile UnitTest
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            10.0
      - uses: actions/checkout@v4
      - name: 'Run: Compile, UnitTest'
        run: ./build.cmd Compile UnitTest
  macos-latest:
    name: macos-latest
    runs-on: macos-latest
    steps:
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            10.0
      - uses: actions/checkout@v4
      - name: 'Run: Compile, UnitTest'
        run: ./build.cmd Compile UnitTest
