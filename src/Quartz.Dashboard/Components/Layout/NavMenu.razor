@using Microsoft.Extensions.Options
@inject NavigationManager NavigationManager
@inject IOptions<QuartzDashboardOptions> Options
@implements IDisposable

<div class="qz-sidebar-brand">
    <svg class="qz-sidebar-brand-logo" viewBox="0 0 28 28" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <circle cx="14" cy="14" r="13" stroke="currentColor" stroke-width="2" />
        <circle cx="14" cy="14" r="3" fill="currentColor" />
        <line x1="14" y1="3" x2="14" y2="8" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
        <line x1="14" y1="20" x2="14" y2="25" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
        <line x1="3" y1="14" x2="8" y2="14" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
        <line x1="20" y1="14" x2="25" y2="14" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
    </svg>
    <span class="qz-sidebar-brand-text">Quartz.NET</span>
</div>

<nav>
    <ul class="qz-sidebar-nav">
        <li class="qz-sidebar-section">Overview</li>

        <li class="qz-nav-item">
            <a class="qz-nav-link @NavClass("")" href="@Link("")">
                <svg class="qz-nav-icon" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <rect x="1" y="1" width="7" height="7" rx="1" />
                    <rect x="10" y="1" width="7" height="7" rx="1" />
                    <rect x="1" y="10" width="7" height="7" rx="1" />
                    <rect x="10" y="10" width="7" height="7" rx="1" />
                </svg>
                <span class="qz-nav-link-text">Dashboard</span>
            </a>
        </li>

        <li class="qz-sidebar-section">Management</li>

        <li class="qz-nav-item">
            <a class="qz-nav-link @NavClass("jobs")" href="@Link("jobs")">
                <svg class="qz-nav-icon" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <rect x="2" y="2" width="14" height="14" rx="2" />
                    <line x1="6" y1="6" x2="12" y2="6" />
                    <line x1="6" y1="9" x2="12" y2="9" />
                    <line x1="6" y1="12" x2="9" y2="12" />
                </svg>
                <span class="qz-nav-link-text">Jobs</span>
            </a>
        </li>

        <li class="qz-nav-item">
            <a class="qz-nav-link @NavClass("triggers")" href="@Link("triggers")">
                <svg class="qz-nav-icon" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <circle cx="9" cy="9" r="7" />
                    <polyline points="9,5 9,9 12,11" />
                </svg>
                <span class="qz-nav-link-text">Triggers</span>
            </a>
        </li>

        <li class="qz-nav-item">
            <a class="qz-nav-link @NavClass("calendars")" href="@Link("calendars")">
                <svg class="qz-nav-icon" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <rect x="2" y="3" width="14" height="13" rx="2" />
                    <line x1="2" y1="7" x2="16" y2="7" />
                    <line x1="6" y1="1" x2="6" y2="4" />
                    <line x1="12" y1="1" x2="12" y2="4" />
                </svg>
                <span class="qz-nav-link-text">Calendars</span>
            </a>
        </li>

        <li class="qz-sidebar-section">Monitoring</li>

        <li class="qz-nav-item">
            <a class="qz-nav-link @NavClass("executing")" href="@Link("executing")">
                <svg class="qz-nav-icon" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <polygon points="6,3 15,9 6,15" />
                </svg>
                <span class="qz-nav-link-text">Executing</span>
            </a>
        </li>

        <li class="qz-nav-item">
            <a class="qz-nav-link @NavClass("history")" href="@Link("history")">
                <svg class="qz-nav-icon" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <polyline points="1,9 4,6 7,9" />
                    <path d="M4,6 L4,13 A5,5 0 1,0 14,13" />
                    <polyline points="12,10 12,13 15,13" />
                </svg>
                <span class="qz-nav-link-text">History</span>
            </a>
        </li>

        <li class="qz-nav-item">
            <a class="qz-nav-link @NavClass("live")" href="@Link("live")">
                <svg class="qz-nav-icon" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <path d="M4,4 A7.07,7.07 0 0,1 14,4" />
                    <path d="M6,7 A4.24,4.24 0 0,1 12,7" />
                    <circle cx="9" cy="11" r="1.5" fill="currentColor" />
                </svg>
                <span class="qz-nav-link-text">Live Logs</span>
            </a>
        </li>

        <li class="qz-nav-item">
            <a class="qz-nav-link @NavClass("actions")" href="@Link("actions")">
                <svg class="qz-nav-icon" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <line x1="3" y1="4" x2="15" y2="4" />
                    <line x1="3" y1="9" x2="15" y2="9" />
                    <line x1="3" y1="14" x2="15" y2="14" />
                    <circle cx="6" cy="4" r="1" fill="currentColor" />
                    <circle cx="12" cy="9" r="1" fill="currentColor" />
                    <circle cx="8" cy="14" r="1" fill="currentColor" />
                </svg>
                <span class="qz-nav-link-text">Action Log</span>
            </a>
        </li>
    </ul>
</nav>

@code
{
    private string? _currentRelativePath;

    protected override void OnInitialized()
    {
        UpdateCurrentRelativePath(NavigationManager.Uri);
        NavigationManager.LocationChanged += OnLocationChanged;
    }

    private string Link(string subPath)
    {
        return DashboardLink.To(Options.Value, subPath);
    }

    private void UpdateCurrentRelativePath(string uri)
    {
        // Resolved once per location change (NavClass runs for every nav link on every render):
        // ToDashboardRelativePath handles both the prerender and the interactive base URI shapes
        _currentRelativePath = DashboardLink.ToDashboardRelativePath(uri, NavigationManager.BaseUri, Options.Value);
    }

    private string NavClass(string route)
    {
        string? relativePath = _currentRelativePath;
        if (relativePath is null)
        {
            return string.Empty;
        }

        // Exact match for dashboard root, prefix match for sub-pages
        if (route.Length == 0)
        {
            return relativePath.Length == 0 ? "qz-nav-active" : string.Empty;
        }

        bool isActive = relativePath.Equals(route, StringComparison.OrdinalIgnoreCase)
            || relativePath.StartsWith(route + "/", StringComparison.OrdinalIgnoreCase);

        return isActive ? "qz-nav-active" : string.Empty;
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        UpdateCurrentRelativePath(e.Location);
        InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= OnLocationChanged;
    }
}
